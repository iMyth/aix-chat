/**
 * 聊天配置文件
 *
 * 在这里注册你的卡片组件映射、工具显示名、Transport 配置。
 * 将此配置传给 <AiChatProvider :config="chatConfig">
 */
import { defineChatConfig } from 'aix-chat'

// 导入你的卡片组件
// import OptionSelectorCard from './components/OptionSelectorCard.vue'
// import SimpleFormCard from './components/SimpleFormCard.vue'
// import SimpleListCard from './components/SimpleListCard.vue'

export const chatConfig = defineChatConfig({
  cards: {
    pending: {
      // 当 AI 工具返回 { type: 'xxx', ... } 时，渲染对应的卡片组件
      // exampleType: OptionSelectorCard,
    },
    completed: {
      // 已完成的工具结果（只读模式显示在消息历史中）
      // queryExample: SimpleListCard,
    },
  },

  toolDisplayNames: {
    // 工具调用指示器显示的 i18n key
    // exampleTool: 'tool.exampleTool',
  },

  transport: {
    api: '/api/chat/mobile',
    headers: {
      'x-api-key': import.meta.env.VITE_APP_AI_KEY || '',
    },
    // buildBody: ({ context, sender }) => ({ context, sender, extraField: '...' }),
  },

  voice: {
    enabled: true,
    sttEndpoint: '/api/stt/open',
  },

  welcome: {
    text: '你好！有什么可以帮你的？',
    quickReplies: [
      { label: '操作 A', value: '执行操作 A' },
      { label: '操作 B', value: '执行操作 B' },
    ],
  },
})
